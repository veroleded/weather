import React from 'react'
import { Post } from '../redux/slices/post';

const Post = (props: Record<string, Post>) => {
  return (
    <div className='wrapper-post'>
    </div>
  )
}

export default Post